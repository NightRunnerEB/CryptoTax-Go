apiVersion: v1
kind: Service
metadata:
  name: price-svc
spec:
  type: {{ .Values.service.type }}
  selector:
    app: price-svc
  ports:
    - name: grpc
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
