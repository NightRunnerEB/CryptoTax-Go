apiVersion: v1
kind: Service
metadata:
  name: report-svc
spec:
  type: {{ .Values.service.type }}
  selector:
    app: report-svc
  ports:
    - name: grpc
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
