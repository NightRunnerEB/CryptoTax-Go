replicaCount: 1

strategy:
  maxUnavailable: 0
  maxSurge: 1

image:
  repository: cryptotax/tax-svc
  tag: dev
  pullPolicy: Never

service:
  type: ClusterIP
  grpcPort: 8096
  httpPort: 8097

probes:
  startup:
    enabled: true
    periodSeconds: 5
    timeoutSeconds: 3
    failureThreshold: 30
  readiness:
    enabled: true
    initialDelaySeconds: 8
    periodSeconds: 10
    timeoutSeconds: 3
    failureThreshold: 3
  liveness:
    enabled: true
    initialDelaySeconds: 20
    periodSeconds: 15
    timeoutSeconds: 3
    failureThreshold: 3

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

podSecurityContext:
  enabled: true
  runAsNonRoot: true
  runAsUser: 65532
  runAsGroup: 65532
  fsGroup: 65532
  fsGroupChangePolicy: OnRootMismatch

containerSecurityContext:
  enabled: true
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL

secrets:
  name: tax-svc-secrets

externalSecret:
  enabled: true
  name: tax-svc-externalsecret
  refreshInterval: 1m
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    creationPolicy: Owner
    deletionPolicy: Retain
  vault:
    key: cryptotax/tax-svc
  data:
    - secretKey: APP_VERSION
      property: APP_VERSION
    - secretKey: APP_ENV
      property: APP_ENV
    - secretKey: DATABASE_URL
      property: DATABASE_URL
    - secretKey: AGGREGATION_SVC_ADDR
      property: AGGREGATION_SVC_ADDR
    - secretKey: REPORT_SVC_ADDR
      property: REPORT_SVC_ADDR
    - secretKey: MINIO_ENDPOINT
      property: MINIO_ENDPOINT
    - secretKey: MINIO_PUBLIC_BASE_URL
      property: MINIO_PUBLIC_BASE_URL
    - secretKey: MINIO_ACCESS_KEY
      property: MINIO_ACCESS_KEY
    - secretKey: MINIO_SECRET_KEY
      property: MINIO_SECRET_KEY
    - secretKey: OTEL_EXPORTER_OTLP_ENDPOINT
      property: OTEL_EXPORTER_OTLP_ENDPOINT
    - secretKey: OTEL_EXPORTER_OTLP_INSECURE
      property: OTEL_EXPORTER_OTLP_INSECURE
